<Page x:Class="UP.Pages.RecipeDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="RecipeDetailsPage"
      Background="#1E1E1E">

    <Page.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
            <Button Content="🔙 Назад" 
                    Background="#666" 
                    Foreground="White"
                    Padding="20,10"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BackButton_Click"/>
            <TextBlock Text="🍲 Детали рецепта" 
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="20,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Основной контент -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- Заголовок и изображение -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock x:Name="RecipeTitleText"
                                   Text="Название рецепта"
                                   FontSize="22"
                                   FontWeight="Bold"
                                   Foreground="#4CAF50"
                                   Margin="0,0,0,10"/>
                        <Image x:Name="RecipeImage"
                               Height="220"
                               Stretch="UniformToFill"
                               Margin="0,0,0,10"
                               ClipToBounds="True"/>
                        <TextBlock x:Name="RecipeDescriptionText"
                                   Text="Краткое описание рецепта."
                                   FontSize="14"
                                   Foreground="#CCCCCC"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"/>
                    </StackPanel>
                </Border>

                <!-- Ингредиенты -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="🧂 Ингредиенты"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,0,0,10"/>
                        <ItemsControl x:Name="IngredientsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="• {Binding}" Foreground="#CCCCCC" FontSize="14" Margin="5,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Шаги приготовления -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="👩‍🍳 Шаги приготовления"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,0,0,10"/>
                        <ItemsControl x:Name="StepsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="{Binding StepNumber}" 
                                                   Foreground="#4CAF50"
                                                   FontWeight="Bold"
                                                   Width="25"/>
                                        <TextBlock Text="{Binding StepText}" 
                                                   Foreground="#CCCCCC"
                                                   TextWrapping="Wrap"
                                                   FontSize="14"
                                                   Width="700"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Нижняя панель -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="🛒 Добавить в список покупок" Style="{StaticResource PrimaryButton}" Click="AddToShoppingList_Click"/>
            <Button Content="⭐ В избранное" Style="{StaticResource PrimaryButton}" Click="AddToFavorites_Click"/>
        </StackPanel>

    </Grid>
</Page>
