<Page x:Class="UP.Pages.RecipeDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="RecipeDetailsPage"
      Background="#1E1E1E"
      d:DesignHeight="800" d:DesignWidth="450">

    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="StatPillStyle" TargetType="Border">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="🡠 Назад" 
                    Background="Transparent" 
                    Foreground="#AAAAAA" 
                    BorderThickness="0" 
                    FontSize="16"
                    Cursor="Hand"
                    Click="BackButton_Click"/>

            <TextBlock Grid.Column="1" 
                       Text="Детали рецепта" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
            <StackPanel>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock x:Name="RecipeTitleText"
                                   Text="Название блюда"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#4CAF50"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,15"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <Border Style="{StaticResource StatPillStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⏱ " Foreground="#AAAAAA"/>
                                    <TextBlock x:Name="PrepTimeText" Text="45 мин" Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource StatPillStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🔥 " Foreground="#AAAAAA"/>
                                    <TextBlock x:Name="CaloriesText" Text="350 ккал" Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource StatPillStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🥗 " Foreground="#AAAAAA"/>
                                    <TextBlock x:Name="IngredientsCountText" Text="8 инг." Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <Border CornerRadius="8" Margin="0,0,0,15">
                            <Image x:Name="RecipeImage"
                                   Stretch="Uniform"
                                   MaxHeight="300"/>
                        </Border>

                        <TextBlock x:Name="RecipeDescriptionText"
                                   Text="Описание рецепта..."
                                   FontSize="16"
                                   Foreground="#DDDDDD"
                                   TextWrapping="Wrap"
                                   LineHeight="24"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="🧂 Ингредиенты" Style="{StaticResource SectionHeaderStyle}"/>

                        <ItemsControl x:Name="IngredientsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" 
                                                   Text="{Binding Name}" 
                                                   Foreground="#E0E0E0" 
                                                   FontSize="16"
                                                   VerticalAlignment="Bottom"/>

                                        <Rectangle Grid.Column="0" 
                                                   Height="1" 
                                                   Stroke="#33FFFFFF" 
                                                   StrokeDashArray="4 2" 
                                                   StrokeThickness="1"
                                                   VerticalAlignment="Bottom"
                                                   Margin="5,0,5,3"
                                                   RenderOptions.EdgeMode="Aliased"/>

                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Amount}" 
                                                   Foreground="#4CAF50" 
                                                   FontWeight="Bold"
                                                   FontSize="16"
                                                   VerticalAlignment="Bottom"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="👨‍🍳 Приготовление" Style="{StaticResource SectionHeaderStyle}"/>

                        <ItemsControl x:Name="StepsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Background="Transparent" 
                                                BorderBrush="#4CAF50" 
                                                BorderThickness="2" 
                                                CornerRadius="50" 
                                                Width="32" Height="32" 
                                                HorizontalAlignment="Left" 
                                                VerticalAlignment="Top"
                                                Margin="0,2,15,0">
                                            <TextBlock Text="{Binding Number}" 
                                                       Foreground="#4CAF50" 
                                                       FontWeight="Bold" 
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"/>
                                        </Border>

                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Text}" 
                                                   Foreground="#DDDDDD" 
                                                   FontSize="16"
                                                   LineHeight="24"
                                                   TextWrapping="Wrap"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" 
                Background="#1E1E1E" 
                BorderBrush="#333333" 
                BorderThickness="0,1,0,0" 
                Padding="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="В список покупок" 
                        Style="{StaticResource PrimaryButton}" 
                        Background="#5cb82e"
                        Click="AddToShoppingList_Click"/>

                <Button Content="⭐ В избранное" 
                        Style="{StaticResource PrimaryButton}" 
                        Background="#FFC107"
                        Foreground="Black"
                        Click="AddToFavorites_Click"/>
            </StackPanel>
        </Border>

    </Grid>
</Page>