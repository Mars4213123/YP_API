<Page x:Class="UP.Pages.ShoppingListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Список покупок"
      Background="#1E1E1E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="🛒 Список покупок" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White"/>

            <Button Content="🔄 Сгенерировать из меню" 
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="15,10"
                    Margin="20,0,0,0"
                    Click="GenerateFromMenu_Click"/>
        </StackPanel>

        <!-- Список покупок -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ShoppingItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2D2D30" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="{Binding Category}" 
                                           FontSize="16" 
                                           FontWeight="SemiBold" 
                                           Foreground="#4CAF50"
                                           Margin="0,0,0,10"/>

                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Name}" 
                                                      Foreground="White"
                                                      Margin="5,2"
                                                      Checked="ItemChecked"
                                                      Unchecked="ItemUnchecked"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="📧 Поделиться" 
                    Background="#2196F3"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="15,10"
                    Margin="0,0,10,0"
                    Click="ShareList_Click"/>

            <Button Content="⏰ Напоминание" 
                    Background="#FF9800"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="15,10"
                    Click="SetReminder_Click"/>
        </StackPanel>
    </Grid>
</Page>